import React from 'react';
import styles from './input-styles';
import withStyles from 'react-jss';

const Input = (props) => {
  const {
    id,
    label,
    element,
    placeholder,
    validators,
    initialValue,
    errorText,
    onInput
  } = props;
};
